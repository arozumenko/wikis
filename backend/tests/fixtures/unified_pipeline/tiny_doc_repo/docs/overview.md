# System Overview

This document describes the high-level architecture of the system.

## Components

The system consists of three main services:

- **Auth Service** — handles user login and session management
- **Data Service** — persists and retrieves user data
- **API Gateway** — routes incoming HTTP requests to downstream services
